# Part 1.B — Fine-tuning GPT-2 with LoRA

## Overview
Fine-tune the pre-trained GPT-2 model on Penn TreeBank using LoRA (Low-Rank Adaptation).

## TODO
- Implement LoRA adapter (model.py)
- Implement fine-tuning pipeline (functions.py, main.py)
- Run experiments with different LoRA ranks

## How to run
```bash
conda activate nlu26
cd LM/B
python main.py
```
